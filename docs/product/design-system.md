# Дизайн-система CAOS

Основание: разбор критики (Шаги 32–36). Принцип: **ясность → структура → действие → контекст**. Это не карточка как единица интерфейса, а процесс деятельности.

## Токены

Все цвета задаются семантическими переменными из `src/styles/tokens.css` — компоненты никогда не пишут hex. Тёмная тема включается автоматически через `prefers-color-scheme` и переопределяет только значения переменных.

### Поверхности
| Токен | Свет | Назначение |
|---|---|---|
| `--background` | `#f5f7fb` | фон приложения |
| `--surface` | `#ffffff` | панели/карточки |
| `--surface-alt` | `#fbfcfe` | чередование секций |
| `--surface-muted` | `#eef1f8` | hover, приглушённые блоки |

### Текст
`--text` (основной), `--text-secondary`, `--text-muted`.

### Семантика цвета — жёсткое правило
- **Зелёный (`--success`)** = подтверждено/достигнуто. Никогда — «новое».
- **Красный (`--danger`)** = оспорено/отклонено/ошибка.
- **Фиолетовый (`--ai`)** = **только AI**. Ни один человеческий элемент не окрашивается фиолетовым.
- **`--warning`** = внимание без ошибки.
- Кнопки на primary-поверхностях используют `--on-primary` (постоянный белый).

### Статусы — `ui/CaosStatus`

Единственный способ показать статус: `<CaosStatus status={...} label={...?} />`. Цвет по словарю: зелёный (active, verified, accepted, fulfilled, completed, qualified), красный (rejected, failed, cancelled, disputed), синий (in_progress, proposed, under_review, voting, reported), жёлтый (suspended, achieved, deferred, acknowledged, open), серый (draft, closed, abandoned, superseded, withdrawn). Текст всегда сопровождает цвет — статус обязан объяснять себя без опоры на оттенок. Проверено computed styles на живых данных.

### Радиусы, отступы, типографика
`--radius-sm/md/lg` (6/8/12px), шкала `--space-1..8` (4px база), шкала шрифта `--font-display..--font-meta`. Применение шкалы — по мере касания компонентов (не массовая замена).

## Правила (запреты из критики)

1. Никакого glassmorphism, градиентных свечений и «glowing AI».
2. Skeleton вместо спиннеров при загрузке списков.
3. Статус обязан объяснять состояние (не только цвет — и текст).
4. Осторожный optimistic UI: реакцию можно ставить оптимистично, коллективные решения — никогда.
5. Запрет UniversalCard/EntityCard: каждый доменный компонент отражает смысл сущности (GoalHeader, CommitmentCard...).

## Статус внедрения

- ✅ Токены + тёмная тема (автоматическая), весь styles.css переведён на переменные (остался только брендовый Google-синий `#4285f4`).
- ✅ Проверено визуально в браузере: светлая и тёмная темы страницы входа.
- ✅ Типографическая дисциплина: три варианта h2 консолидированы в `--font-title`, h1 — `--font-display`, eyebrow/meta — `--font-meta`; чтение текста ограничено мерой `--measure` (720px).
- ✅ Skeleton-состояния: `ui/Skeleton` + правило «только пока данных нет вовсе» (после мутаций список уже заполнен — мигания нет).
- ⬜ Плотности (compact/default/relaxed) — по мере касания компонентов.
